{
  "name": "web",
  "author": "Adrian Gesto",
  "license": "MIT",
  "version": "1.0.0",
  
  "description": "UI for button pad",
  "type":"module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}